public class partyUpdateTriggerHandler {
    public static void appendComment(List<genesis__Application_Parties__c> newList ){
      /*  System.debug('update comment field');
        String cmnt = ' ';
        List<genesis__Application_Parties__c> updateList = new List<genesis__Application_Parties__c>();
        genesis__Application_Parties__c prtyRec = new genesis__Application_Parties__c();
        for(genesis__Application_Parties__c prty : newList){
            cmnt = prty.EMI_Comments__c;
            prtyRec.All_Comments__c = cmnt + '\n' + prty.EMI_Comments__c;    
            prtyRec.Id                          = prty.Id;
            prty.EMI_Comments__c               = ' ';
            
           updateList.add(prtyRec);
        }  
        System.debug('Updated Party list"""""'+updateList);
        update updateList(prtyRec);
        */
    } 
}